# Common sources used by both native and Wine components

common_sources = files(
    'shared_memory.cpp',
    'socket.cpp',
)

# Header-only common includes
common_includes = include_directories('.')
